package fr.soifi.ecommerce.dal.entity;

// TODO: enum du statut de commande (PENDING, SHIPPED, DELIVERED, CANCELLED)
public enum OrderStatus {
    PENDING,
    SHIPPED,
    DELIVERED,
    CANCELLED
}
